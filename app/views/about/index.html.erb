<link href="https://fonts.googleapis.com/css?family=Nunito+Sans:900" rel="stylesheet">

<style>
  @media only screen and (max-width: 300px){
    .about-text h1 {
      margin-left:150px !important;
      transform: translate(0px,0px) !important;
    }
    .about-text p{
      margin-left:150px !important;
      width:400px !important;
      font-size: 12px !important;
    }
    .about-text {
      transform: rotate(0deg)!important;
      margin-top:20px !important;
    }
    .about-text button {
      margin-left: 150px !important;
    }
  }
  @media only screen and (max-width: 600px){

    .about-text h1 {
      margin-left:120px !important;
      transform: translate(0px,0px) !important;
    }
    .about-text p{
      margin-left:120px !important;
      width:330px !important;
      font-size:12px !important;
    }
    .about-text {
      transform: rotate(0deg) translateY(-300px)!important;
    }
    .about-text3 {
      margin-top: -300px !important;
      height: auto !important;
    }
    .about-text button {
      margin-left: 150px !important;
    }


    .inner-about {
      transform: rotate(0deg) !important;
    }
    .inner-about {
      transform: rotate(0deg) !important;
    }

    #threejs {
      display: none !important;
    }

    .about-container h1 {
      font-size:70px !important;
      animation:none !important;
      margin-top:200px !important;
    }
  }

  body {
    background: black;
    overflow: hidden;
    animation: changeoverflow 1s 3s 1 forwards;
    overflow-x:hidden;
  }

  @keyframes changeoverflow {
    0%{
      overflow-y: hidden;
    }
    100%{
      overflow-y: auto;
    }
  }

  .about-container {
    width:100%;
    height:100%;
    position: relative;
  }
    .about-container h1 {
      font-size:250px;
      color:#fff367;
      text-align:center;
      z-index: -1;
      position: absolute;
      font-family: 'Nunito Sans', sans-serif;
      right:0;
      left:0;
      top:0;
      bottom:0;
      margin-top: 130px;
      animation: pulse 1s ease-in-out forwards 1 1.3s;

    }

    .changer {
      width:100%;
      position: absolute;
      top:150px;
      left:0;
      right: 0;
      bottom: 0;
      height:270px;
      background:black;
      animation: mover 1s ease-in-out forwards 1 1s;
    }

    @keyframes pulse{
      0%{
        opacity: 1;
        font-size:250px;
      }
      50%{
        opacity: 0.7;
        font-size:253px;
      }
      100%{
        opacity: 1;
        font-size:250px;
      }
    }

    @keyframes mover {
      0%{
        left: 0;
        width: 100%;
      }
      100%{
        left:0;
        width: 0;
      }
    }

    #threejs {
      opacity: 0;
      animation: showIt 1s  ease-in-out forwards 1 2s;
    }

    @keyframes showIt {
      0%{
        opacity: 0;
      }
      100%{
        opacity: 1;
      }
    }

    .about-text {
      margin-top:100px;
      background: rgba(31,31,31,0.4);
      height:100%;
      width:200%;
      margin-left: -100px;
      transform: rotate(10deg);
      position: relative;
    }

    .about-text h1 {
      font-size:15px !important;
      color:white;
      font-family: 'Arial', sans-serif;
      font-weight: bold;
      font-weight: 100;
      text-transform: uppercase;
      z-index: 9999999999999999;
      letter-spacing: 2px;
      transform:translateY(-50px) !important;
      margin-left: 300px;


    }

    .about-text p {
      color:white;
      line-height:35px;
      font-family: Helvetica, sans-serif;
      font-weight: 100;
      letter-spacing: 0.5px;
      margin-left: 200px;
      width:1000px;

    }

    .inner-about {
      position: absolute;
      left: 0;

      width:100%;
      height: auto;
      transform: rotate(-10deg);
    }

    .footer {
      position: fixed !important;
      bottom:0;
    }

    .inner-about button {
      width:250px;
      height:50px;
      background:rgb(31,31,31);
      margin-left:300px;
      margin-top:30px;
      border: none;
      border-radius:.2em;
      color:white;
      font-family: Arial, sans-serif;
      font-weight: 900;
      letter-spacing:1px;
    }



</style>


<%= render 'partials/nav' %>
<div class="about-container">
  <div class="changer"></div>
  <h1 class="parallax">ABOUT </h1>
  <div id="threejs"></div>

</div>

<div class="about-text">
  <div class="inner-about">
  <div class="side-div"></div>
    <h1>What is Perfect Presence? <span style="color: #fff367">&#9698;</span></h1>
  <p >We are a change in the digital landscape for true entrepreneurs and business owners with brand development at its finest. Get everything a business could need to grow all in one place; Web, Mobile, Branding, Marketing, Media and More. Don't just pay to get something created get yourself a partner to see your concept through from start to finish.
    The current online attention market is the driving force to many of the successful and rising brands we see today. That means you cannot afford to be left behind by a company full of empty promises when action is the only way to succeed. With our expert team we have what it takes to compete on any stage to drive real success to your brands digital venture.</p>
  </div>
  </div>



<div style="margin-top:-50px;" class="about-text">
  <div class="inner-about">
    <div class="side-div"></div>
    <h1 style="transform:translateY(0px) !important;">How Does It Work? <span style="color: #fff367">&#9698;</span></h1>
    <p style="margin-top:70px;">Creating the Perfect approach to your online Presence is not always easy. Luckily when its with our amazing team the process is extremely simple to make sure while you are making  a new idea or adding on to an existing brand, you can focus on what you want while we take care of all your digital needs.</p>
  </div>
</div>

<div style="margin-top:-50px;background:none; transform: rotate(0deg); height:300px !important;" class="about-text about-text3">
  <div style="transform: rotate(0deg) !important; margin-top:50px;" class="inner-about">
    <div  class="side-div"></div>
    <h1 style="transform:translateY(0px) !important; color:white;">Let's Get Started! <span style="color: #fff367">&#9698;</span></h1>
    <button>Schedule My Consultation</button>
  </div>
</div>

<div class="bottom">

</div>



<script>
    (function() {
        var addMesh, animate, camera, clock, container, controls, draw, mesh, renderer, rotate, scene, start;

        scene = void 0;

        camera = void 0;

        renderer = void 0;

        container = void 0;

        controls = void 0;

        clock = void 0;

        mesh = void 0;

        rotate = true;

        start = function() {
            var ASPECT, FAR, NEAR, SCREEN_HEIGHT, SCREEN_WIDTH, VIEW_ANGLE;
            scene = new THREE.Scene;
            VIEW_ANGLE = 45;
            NEAR = 0.1;
            FAR = 10000;
            SCREEN_WIDTH = window.innerWidth;
            SCREEN_HEIGHT = window.innerHeight;
            ASPECT = SCREEN_WIDTH / SCREEN_HEIGHT;
            camera = new THREE.PerspectiveCamera(VIEW_ANGLE, ASPECT, NEAR, FAR);
            camera.position.set(77.82504298529939, 54.02121980722944, -74.02178601347767);
            camera.rotation.set(-2.4020274682629177, 0.6097939469542306, 2.6601321578044113);
            camera.lookAt(scene.position);
            scene.add(camera);
            renderer = new THREE.WebGLRenderer({
                antialias: true,
                alpha: true
            });
            renderer.setSize(SCREEN_WIDTH, SCREEN_HEIGHT);
            container = document.getElementById('threejs');
            container.appendChild(renderer.domElement);
            clock = new THREE.Clock;
        };


        var hemiLight, jsonLoader, light;


        draw = function() {
            light = new THREE.DirectionalLight(0xffffff, 1);
            light.position.set(50, 0, -500);
            light.castShadow = true;
            scene.add(light);
            jsonLoader = new THREE.ObjectLoader;
            return jsonLoader.load('trop/trop.json', addMesh);
        };


        var myObj;
        addMesh = function(object) {

            myObj = object;

            var materialObj = new THREE.MeshNormalMaterial({
                vertexColors: THREE.FaceColors,
                overdraw: 0.5
            });
            object.traverse(function(child) {
                if (child instanceof THREE.Mesh) {
                    child.geometry.computeVertexNormals;
                }
            });

            object.position.set(0,20,0);
            object.scale.set(550,550,550);
            // then directly add the object
            return scene.add(object);
        };

        animate = function() {
            requestAnimationFrame(animate);


            if(myObj)
              myObj.rotation.y += 0.001;
            return renderer.render(scene, camera);
        };




        start();
        var delay = 2000; //milliseconds
        var timeoutId;
        var animationIsFinished = false;

        draw();

        animate();

    }).call(this);






</script>

<script>

</script>